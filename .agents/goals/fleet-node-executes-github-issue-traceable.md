# /goal A fleet node executes a real GitHub issue end to end through the Agent Canvas and the injecting proxy, and the merged PR is traceable to that node at every hop.

— CONTEXT —
· Project: Prove the fleet registry is an agent engineering orchestration harness, not just a proxy. The registry is already proven as a pipe (PRs #1, #2, #3 merged: enrol, pre-seed vs pending TOFU, approve, credential injection, revoke fails closed, fresh-browser hydration, 15/15 live assertions in `tests/e2e/live/fleet-registry/`). The only model turn ever executed on a remote node emitted `ActionEvent [agent] {"message":"FLEET-PROOF-OK","kind":"FinishAction"}` and stopped, so nothing shows a node doing sustained engineering work. Four phases: PHASE 0, the master builds the evidence layer and makes ACP permanent, because the observer must not be built by the observed; PHASE 1, the master files a properly labelled GitHub issue; PHASE 2, claude-hetzner executes it through the canvas with all recording layers active; PHASE 3, the master reviews, sends findings back to the node, and merges. Design spec: `.agents/specs/2026-09-06-fleet-acp-engineering-harness.md`. This goal has been adversarially reviewed; every clause below closing a named finding is load-bearing, not decoration.
· Stack: Node 22 ESM (`scripts/registry/*`, `scripts/proxy-backend.mjs`, `bin/enrol.mjs`); React 19.2.8 + react-router 7.18.2 canvas on Vite 8; `@openhands/typescript-client` 1.39.0 against `openhands-agent-server` 1.44.0; vitest 4.1.10; Playwright 1.62.1 with video and trace; the existing live rig at `tests/e2e/live/fleet-registry/{rig.mjs,fleet-registry.spec.ts,rig-state.ts}`; tailscale; ACP via Claude Code CLI 2.1.261 on Stevie's Max subscription.
· Current state: `origin/main` is at the merge of PR #3. THE TICKET is a genuine gap: `"stale"` appears in `src/api/backend-registry/types.ts:12`, `storage.ts:35`, `registry-source.ts:76` and `src/mocks/registry-handlers.ts`, and in ZERO components, so a node its source stopped reporting renders as healthy and selectable, while `proxy-backend.mjs:299` already 403s anything not `active`, so selecting it gives an unexplained 403. That is the real bug. FR-023 defines the server semantics and was written before this idea existed. The single `## Deferred` item was REJECTED as a ticket because the record argues against building it. `scripts/proxy-backend.mjs` logs nothing today. claude-hetzner (`100.125.222.64`): git 2.43.0, gh 2.95.0 authenticated as `stevengonsalvez` (THE SAME IDENTITY AS THE MASTER, so GitHub cannot distinguish a PR opened from the node from one opened from the Mac; only a node-only signing key can), node v22.23.0, npm 10.9.8, python3 3.12.3, agent server on `127.0.0.1:18000` published at `:8443`, SSH `BatchMode=yes` as `claude`, NO `~/workspace` yet. hetzner is on `agent_kind=openhands` with litellm and NO api key, so IT CANNOT RUN A MODEL until Phase 0 fixes it. THE EXACT ACP RECIPE, recovered from the session that made it work: (a) the config key is `permissions.defaultMode` in `/home/claude/.claude/settings.json` (the Claude Code CLI's own settings, not OpenHands'); `"auto"` FAILS with `ACPInitError: [-32603] Invalid permissions.defaultMode: auto`; `"bypassPermissions"` works and matches the SDK's `default_session_mode` for `claude-agent-acp`; any PROMPTING mode wedges the run silently because the Agent Canvas renders no ACP permission-request UI anywhere in `src/`; `acp_session_mode` in `agent_settings_diff` is a cleaner host-scoped alternative but is UNTESTED. (b) settings must be PATCHed as `{"agent_settings_diff":{"agent_kind":"acp","acp_server":"claude-code","acp_model":"opus[1m]"}}`; a top-level `{"agent_settings":...}` is rejected 400. (c) launch via `GET /api/agent-profiles` for `active_agent_profile_id`, then `POST /api/conversations` with `agent_profile_id`, `workspace`, `initial_message`; omitting agent, agent_settings and agent_profile_id gives 422. (d) restart-proof with `acp_command:["/home/claude/.local/bin/claude"]`; today it works only because the running process happens to have `~/.local/bin` on its PATH. (e) CONFIG PROVES NOTHING: `agent_kind:"acp"` is what the node reports WHILE FAILING AT INIT; liveness is agent-sourced `SystemPromptEvent` plus `ActionEvent` in `/api/conversations/<id>/events/search`, failure code in `.items[].code`. (f) NEVER treat `LLMBadRequestError: LLM Provider NOT provided ... model=opus[1m]` as failure: it appears in SUCCESSFUL runs, it is the title-generation call. RETRACTED, DO NOT PROPAGATE: the claim that an inline `agent_settings` dump silently downgrades to the LLM agent is UNPROVEN and was withdrawn by its author; it is still written in `docs/registry.md` on main and in the PR #3 body; the only isolated defect was a broken event poll, already fixed. Use `agent_profile_id` because it is known to work, not because the dump is known to fail. MagicDNS fails on the Mac, so reach hetzner via `--resolve` or the raw tailnet IP.
· Working dir: /Users/stevengonsalvez/.agents-in-a-box/worktrees/by-name/OpenHands--f-registry-phase1--7ecb84fb
· Constraints: PHASE 2 IS PINNED TO A THROWAWAY RIG INGRESS stood up by `tests/e2e/live/fleet-registry/rig.mjs up` on a random 39000-39999 port, NEVER Stevie's live stack; the evidence log goes to `<rig.dir>/evidence/proxy-access.jsonl`, never `~/.openhands`; do not run `rig.mjs down` until every artefact is copied out and the chain verifier has passed, because teardown clears the secret store and `fleet_backends`. NEVER restart, reconfigure or stop the live agent server on `127.0.0.1:8000` ON THE MAC; hetzner's OWN agent-server unit MAY be restarted, and must be, to prove ACP permanence: record its unit name in Phase 0. THE MASTER MUST NOT TOUCH THE REPOSITORY DURING PHASE 2, only prompt through the canvas, review, and merge; everything from clone to opening the PR runs ON claude-hetzner; review findings go BACK to the node. PHASE 0 CREDENTIAL TRAP: log the INBOUND `req.url` before any rewrite and DELETE `session_api_key` from the query before writing, because the upgrade path carries the node's live key in the URL and a naive log writes a fleet credential to disk. FENCED PATHS, mechanically enforced: before merge run `git diff --name-only origin/main...<pr head>` and intersect with `scripts/proxy-backend.mjs`, `scripts/ingress.mjs`, the Phase 0 access-log module and its tests, `scripts/registry/**`, `tests/e2e/live/fleet-registry/**`, `specs/fleet-registry.md`, `.github/**`, `AGENTS.md`, `.agents/**`, `package.json`, `vitest.config.*`, `eslint.config.js`, `playwright*.config.ts`; ANY intersection is a FAILURE, do not merge. `specs/fleet-registry.md` is the most damaging: editing FR-023 redefines "done" retroactively. The node's legitimate blast radius is `src/components/features/backends/**`, `src/api/backend-registry/**`, `src/i18n/translation.json`, `__tests__/**`, and `docs/registry.md` only. ON THE NODE: `git add -A` and `git add .` are FORBIDDEN, stage by named path; `--force-with-lease` only, max 2; exactly ONE open PR, record its number at M5 and merging any other is a FAILURE; before every push scan the diff for the node's own session key; before merge grep the whole PR diff for both session keys and for `gho_`/`ghp_`/`sk-` prefixes, any hit is a FAILURE and the key is rotated. MERGE: use `gh pr merge --merge` or `--rebase`, NEVER `--squash` (a squash commit is unsigned, untrailered and breaks the chain); `--admin` and any bypass are FORBIDDEN; do NOT add the `live-e2e` label and do NOT submit an approving review before the chain verifies, because both trigger workflows that push `allhands-bot` commits to the node's branch; `allhands-bot` commits are exempt from the chain and must be listed explicitly. HUMAN NOTE: Stevie has EXPLICITLY GRANTED permission in-session for this run to satisfy the `HUMAN:` section of `check_pr_description.py`; the PR body must state plainly that the note was written under that explicit grant, so the record is not misleading. PROGRESS-GATED BUDGET, replacing any wall-clock cap: milestones M0 a real model turn on hetzner over ACP, M1 repo cloned on the node, M2 first signed trailered commit, M3 `npm test` run to completion on the node, M4 branch pushed, M5 PR opened, M6 all `gh pr checks` green, M7 reviewer pass with zero unresolved high or medium, M8 merged and chain verifier exits 0; continue while progressing, a new milestone must land within 60 MINUTES of the last; hard ceilings that end the run regardless: 8 HOURS wall clock, 3 conversations on hetzner, 40 prompts through the canvas, 2 force-pushes, 1 open PR. THRASH DETECTOR: record a one-line root cause per failed attempt; THREE consecutive attempts naming the same root cause, or any 60-minute window with no milestone advance, means WEDGED: stop prompting, write a block report naming the milestone, the root cause, the last three attempts and the exact next diagnostic step, and exit. WEDGED is a legitimate terminal state; continuing to prompt past it is the failure. A dead ACP session may be replaced by a NEW conversation on the SAME node within the 3-conversation ceiling, each new id appearing in the proxy log and in its commits' `Fleet-Conversation`; moving to a different machine is fatal and destroys the only claim this run exists to make. PHASE 0 PRE-FLIGHT on hetzner, all must pass before Phase 1: `df -h /home` at least 10 GB free (re-check before M3, ENOSPC is a hard stop not a retry), `gh auth status` with `repo` scope (re-check before M4), `mkdir -p ~/workspace/project`, agent-server unit name recorded. Video must run for ALL of Phase 2; if the browser context restarts, retain every `video.webm` and index segments in `evidence/video-manifest.json` with start, end and conversation id; a fragment of the last five minutes is not the deliverable. Do not render API keys or credential-bearing errors into the browser; follow the live-artefact redaction rules in AGENTS.md.
· Audience: Internal. Stevie and fleet operators running the Agent Canvas across several machines; there is no external end user.

— SUCCESS CRITERIA (ALL MUST BE TRUE) —
1. Phase 1 files an issue on `wololo-labs/OpenHands` from the Feature Request template, labelled `enhancement`, containing a 16-hex-byte run nonce, a `## Desired Behavior` section, and a `## Acceptance Criteria` section with at least these four checkboxes: a `stale` entry renders in the switcher with a distinct label naming the reason and is not silently selectable (refused like `pending` per FR-012, or gated behind explicit confirmation, because `/backend/:id/*` already 403s anything not `active`); the Manage Backends row shows the state label plus last-seen time alongside provenance; coverage drives the state through `registry-source`/`storage` from a registry payload with `state:"stale"`, NOT by passing `state` as a component prop; `specs/fleet-registry.md` FR-023 is unmodified. The master confirms with `gh issue view <N> --json labels` that `issue-readiness-check.yml` applied `ready-for-dev` BEFORE Phase 2 starts. claude-hetzner alone then clones, reads, edits, runs the suite, self-corrects, commits, pushes and opens exactly one PR titled in Conventional Commits form, ticking `- [x] Feature` under `## Type` and writing `Fixes #<N>`; the master's reviewer findings go back to the node and are fixed by the node; the PR merges.
2. Every commit in `origin/main..<pr head>` not authored by `allhands-bot` passes a four-link chain verified by `scripts/verify-fleet-chain.mjs`, which must exit 0 and whose full output ships in the deliverable: (1) a good SSH signature from the ed25519 key generated ON claude-hetzner in Phase 0, whose private half never left it and whose public half is recorded in `evidence/node-signing-key.pub` before Phase 2, cross-checked with `gh api repos/wololo-labs/OpenHands/commits/<sha> --jq .commit.verification.verified`; (2) `Fleet-Conversation: <cid>` and `Fleet-Run: <nonce>` trailers matching the nonce published in the issue; (3) that `<cid>` present in `<rig.dir>/evidence/proxy-access.jsonl` against an entry whose `entryFingerprint` equals hetzner's SSH host-key fingerprint, at a timestamp before the commit's committer date, with `evidence/tunnel-map.json` recording the SSH `-L` mapping because the rig reaches the node through a tunnel and a bare `127.0.0.1` log line proves nothing; (4) an agent event for `<cid>` naming that commit's subject. The git push hop is explicitly NOT proxy-observable and is proven by link 1 alone. Because the WebSocket yields ONE proxy line per session, the master re-fetches `/api/conversations/<cid>/events/search` over HTTP through the proxy at every milestone. Because all recordings are written on the Mac by this run's own processes, the master posts an issue comment at each milestone containing the `sha256` of the proxy log and the event export so far, and `gh issue view <N> --json comments` ships in the deliverable as a server-timestamped seal. Corroboration that the node did the work: the conversation's `persistence_dir` starts with `/home/claude/`, one bash observation captures `hostname && whoami && pwd && git rev-parse --show-toplevel && tailscale ip -4 && ssh-keygen -lf /etc/ssh/ssh_host_ed25519_key.pub` matching the registry entry's fingerprint, and `git log --all --format=%H` in the master's worktree shows the node's branch head absent locally when the PR opens. Any failing row is a harness FAILURE even if the PR is green.
3. ACP is PERMANENT on claude-hetzner, not flipped for the run: `permissions.defaultMode` is `bypassPermissions` (never a prompting mode, which wedges silently), `agent_kind=acp` with `acp_server=claude-code` and `acp_command:["/home/claude/.local/bin/claude"]` are persisted via `agent_settings_diff`, and after RESTARTING hetzner's own agent-server unit a brand-new conversation launched from the canvas with `agent_profile_id` produces an agent `SystemPromptEvent` and `ActionEvent` with no per-run reconfiguration and no API key. The merge gate: `gh pr checks <N> --json name,bucket,link` pasted raw into the report with every check `pass` or `skipping`, the fenced-path intersection empty, the secret grep clean, and `evidence/review-round-<n>.md` written BEFORE each fix with every finding given an id and the follow-up review resolving each id by name.
4. Final deliverable runs without errors
5. You can show proof: the merged PR URL, the issue URL, `scripts/verify-fleet-chain.mjs` output, `gh pr checks` JSON, and the Phase 2 video manifest.

— OPERATING RULES — NON-NEGOTIABLE —
1. PLAN FIRST. Output a numbered task list before writing any code.
2. WORK AUTONOMOUSLY. Don't ask clarifying Qs unless genuinely blocked.
3. SELF-VERIFY. After every step: run tests, inspect output, confirm it worked.
4. DEBUG YOURSELF. If it fails, diagnose + fix. Don't hand it back.
5. USE EVERY TOOL. MCPs · terminal · web · code exec · pull real data.
6. NO PLACEHOLDERS. No TODOs · no stubs · real components + real states.
7. PROGRESS LOG. Track completed · in-flight · decisions · blockers.
8. STAY ON GOAL. Discoveries off-spec? Note + keep moving.
9. IF BLOCKED. Log the wall · continue everything parallelizable.
10. CHECK SUCCESS BEFORE STOPPING. Re-read criteria · confirm each is met.

— QUALITY BAR —
· Code: clean, typed, follows project conventions
· Design: looks like a well-funded startup shipped it
· Output: survives a senior code review
· Docs: every new pattern / env var / decision logged

— FINAL DELIVERABLE —
✅ Confirmation each criterion is satisfied
📂 Every file created / modified
🚀 How to run / test / deploy
📊 Proof (screenshot · test output · URL)
📝 Decisions made + anything to know
⚠️ Known limitations + follow-ups

Begin by outputting your plan. Then execute end-to-end without checking
in until done or genuinely blocked.
